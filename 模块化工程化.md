# 模块化 & 工程化

模块化的最终目的是将程序划分为一个个小的结构
- 在每个结构中编写属于自己的逻辑代码，有自己的作用域，定义变量名词时不会影响到其他结构
- 每个结构可以将自己希望暴露的变量、函数、对象等导出给其他结构使用
- 也可以通过某种方式，导入其他结构中的变量、函数、对象等

| 规范 | 特点 | API | 代表 |
| :---: | :---: | :---: | :---: |
| CommonJS | 同步加载 | module.exports / require | NodeJS| 
| AMD | 异步加载、依赖前置 | define / require | RequireJS |
| CMD | 异步加载、依赖就近 | define / require.async | SeaJS |
| ES6 Module | 静态加载，编译时进行静态分析 | export / import | ES6| 

JS 支持多种不同的模块规范，每种规范都有其特定的使用场景和优势，虽然 ES6 Module 已经逐渐成为主流，但是我们的组件库还是应该尽可能 **导出多种模块规范的文件，以适配不同的需求场景**

可以通过配置打包工具的相关属性来满足不同的模块规范

## CommonJS 和 Node

CommonJS 是 **一个规范**，最初踢出来是在浏览器以外的地方使用，并且当时被命名为 ServerJS，后来为了体现它的广泛性，修改为 CommonJS，简称为 CJS

- Node 是 CommonJS 在服务器端一个具有代表性的实现
- Browserify 是 CJS 在浏览器中的一种实现
- Webpack 具备对 CJS 的支持和转换

CJS 规范：
1. 模块中导出内容用 `exports`
2. 模块中导入内容用 `require`

`exports` 是一个对象，可以添加多个属性进行导出

浏览器没有对 CJS 进行实现，所以在浏览器中默认情况下不能直接使用 CJS。**但是 Node 支持**

### CJS 的缺点

加载模块是同步的：必须等到对应的模块加载完毕，当前模块中的内容才能被运行。

1. 在服务器不会有什么问题，因为服务器加载的 JS 文件都是本地文件加载速度非常快
2. 但是如果在浏览器中，浏览器加载 JS 文件需要先从服务器中将文件下载下来，同步执行意味着会带来阻塞，即使只是一些简单的 DOM 操作。所以一般在浏览器中不使用 CJS 规范
3. 如果在 webpack 中使用 CJS 就没关系，因为它可以将代码转成浏览器可以直接执行的代码

### module.exports

**在实际开发中用 module.exports 比较多**

```js
module.exports = {
    UTIL_NAME,
    getData,
}

// 注意这里，即使重新赋值也不会改变 UTIL_NAME 的值，因为现在 exports 和 module.exports 的内存是两个不同的地址
exports.UTIL_NAME = "别动我"
// 这里就可以修改成功
module.exports.UTIL_NAME = "你把我改了"
```

module.exports 和 exports 的区别见视频 - Node 中 module 的 exports 属性本质，需要从内存角度出发

追根溯源，从 Wikipedia 中对 CJS 规范的解析：
1. CommonJS 中是没有 module.exports 的概念的
2. 但是为了实现模块的导出，Node 中使用的是 Module 类，每一个模块都是 Module 的一个实例，也就是 module
3. 所以在 Node 中真正用于导出的其实根本不是 exports，而是 module.exports
4. **module 才是真正的导出实现者**

那么为什么 exports 也可以导出呢？

因为在 module.exports 没有被赋值新对象时，module.exports = exports，一旦 module.exports 被赋值一个新对象后，二者内存就不一样了也就不相等了

## require 函数解析

require() 可以帮助我们引入一个文件/模块中导出的对象

require(X) 的查找规则如下：
- X 是 Node 核心模块，比如 path、http：直接返回核心模块并且停止查找 `const path = require("path")`
- X 以 ./ 或者 ../ 或者 / 开头：
  - 将 X 当成文件去查找
    - 如果有后缀名，按照后缀名的格式查找对应文件
    - 如果没有后缀名，按照 文件 X -> X.js -> X.json -> X.node 的顺序查找
  - 将 X 作为一个目录查找
    - 查找目录下面的 index 文件，如果没有，按照 X/index.js -> X/index.json -> X/index.node 顺序查找
  - 都没有找到，报错 Not Found
- X 不是路径也不是核心模块
  - 回去 node_modules 中寻找 X
  - 如果当前目录下没有就一层一层向上查找
  - 最后找不到就报错 Not Found

## 模块的加载过程

1. 模块在第一次被引入时，模块中的 JS 代码会被运行一次
2. 模块被多次引入时，会进行缓存，最终只加载（运行）一次
   1. 因为每个模块对象 module 都一个 `loaded` 属性，false 没有加载 true 已经加载
3. 如果有循环引入
   1. 本质上就是一个图，Node 采用 DFS
   2. 比如下图中的模块循环引入，加载顺序是：main - aaa - ccc - ddd - eee - bbb

![模块循环引用示意图](imgs/%E6%A8%A1%E5%9D%97%E5%BE%AA%E7%8E%AF%E5%BC%95%E7%94%A8%E7%A4%BA%E6%84%8F%E5%9B%BE.png)

## ESModule

### 基本用法

ES Module 导入导出使用 `import` 和 `export` 关键字

另一方面它采用 **编译器的静态分析**，并且也加入了 **动态引用** 的方式

**采用 ES Module 将自动采用严格模式 `use strict`**

```js
export { name, age, sayHello }
// 也可以在导出的同时起别名
export {
  name as fname,
  age as fage
}
// 或者直接在定义的时候直接导出
export const name = "lucy"
export const age = 28 

// 另一文件
// 注意如果是在浏览器中使用，这里的后缀名 .js 必须要加
// 如果使用 webpack 打包则可以不用加，因为 webpack 会自动加上。
import { name, age, sayHello } from "./foo.js"
// 导入时也可以给标识符起别名，一般都在导入时起别名
import {
  name as fname,
  age as age
} from "./foo.js"

// 也可在导入时给整个模块起别名
import * as foo from "./foo.js"
```

```html
<script src="./foo.js" type="module"></script>
```

### import 和 export 组合使用

在某些框架中会将 import 和 export 都写在同一个出口文件 index.js 中，方便调用

```js
// index.js
import { formatCount, formatDate } from "./format.js"
import { parseLyric } from "./parse.js"

export {
  formatCount, formatDate, parseLyric
}

// main.js
import { formatCount, formatData, parseLyric } from "./index.js"
```

另外还可以在同一个语句中使用导入导出

```js
// 从 format.js 中导入，再在当前文件中直接导出 formatCount 和 formatDate
export { formatCount, formatDate } from "./format.js"

// 或者直接
export * from "./format.js"
```

### default 用法

前面的导出都是有名字的导出（named exports），还有一种到处叫默认导出 default export

默认到处不需要制定名字，导入时不需要使用 { }，并且可以自己指定名字，方便我们和 CJS 规范相互操作

> **一个模块只能有一个默认导出**

```js
// parseLyric.js
function parseLyric() {
  return ['歌词']
}
const name = "月亮代表我的心"

// 默认导出，导出的只是函数
export default parseLyric
```

```js
// 这里的 lyric 只是一个标识符，可以随便起名字
import lyric from "./parseLyric.js"

console.log(lyric())  // 月亮代表我的心
```

甚至在导出时都不需要起名

```js
// parseLyric.js
export default function() {
  return ['新歌词']
}
// 导入同理
```

### import 函数

正常情况下，导入模块必须放在代码顶部，不能放在代码逻辑中

如果需要在某些逻辑成立时才导入模块，那么就要用到 import 函数

- `import()` 返回的是一个 promise
- `import()` 是异步执行的

```js
let flag = true
if (flag) {
  import("./foo.js").then(res => {
    console.log(res.name, res.age)
  })
  console.log("---------")
}
```

###  ESModule 解析流程 

[参考文章](https://hacks.mozilla.org/2018/03/es-modules-a-cartoon-deep-dive/)

1. 构建 Construction：根据地址查找 JS 文件，并且下载，将其解析成模块记录 Module Record
2. 实例化 Instantiation：对模块记录进行实例化，并且分配内存空间，解析模块的导入和导出语句，把模块指向对应的内存地址
3. 运行 Evaluation：运行代码，计算值，并且将值填充到内存地址中

## NPM

包管理工具 npm 全程 Node Package Manager，现在不仅仅是 Node 包管理了

npm 属于 node 管理工具，所以我们在使用 npm 之前要先安装 node

[官网](https://www.npmjs.org)

### package.json

每个项目都会有一个对应的配置文件，记录项目的名称、版本号、项目描述、依赖库等信息

- 手动从零创建：`npm init -y` （-y 表示所有配置选项都是默认项，不加 -y 就是逐个自己选择）
- 通过脚手架（比如 Vue CLI）创建，脚手架会帮助我们生活才能 package.json，并且里面自动带有相关配置

#### 常见属性

- `name`：项目名称（必填）
- `version`：当前版本号（必填） 
- `description`：描述信息
- `author`：作者（一般在发布时用）
- `license`：开源协议（发布时用）
- `private`：是否私有（如果为 true，那么 npm 就不能发布它），这是防止私有项目或模块发布出去的方式
- `main`：设置程序入口，如果有 main 属性，实际上是找到对应的 main 属性的查找文件
- `scripts`：配置一些脚本指令，以键值对的形式存在
  - 配置后通过 `npm run` 执行
  - `npm start` 和 `npm run start` 的区别：它们是等价的，对于常用的 start、test、stop、restart可以省略 run 直接通过 npm start 运行
- `dependencies`：指定无论开发环境还是生成环境都需要依赖的包
  - 通常是我们项目实际用到的一些库模块，比如 vue、vuex、vue-router……
  - 与之对应的是 devDependencies 
  - 通常在上传代码时是不需要上传 node-modules 的，跑代码之前 `npm install` 就可以将 dependencies 中所有的依赖下载到 node-modules 中
- `devDependencies`：开发环境依赖，一些包在生成环境是不需要的，比如 webpack、babel 等，这个时候就可以通过 `npm install webpack --save-dev` 将它安装到 devDependencies 中，或者直接简写 `npm install webpack -D`
- `peerDependencies`：对等依赖，也就是当前的包会依赖于另外一个包，依赖的另外一个宿主包就写在这里。比如 element-plus 就依赖于 Vue3，AntDesign 就依赖于 React

```json
{
  "scripts": {
    "start": "node ./src/main.js",
    "build": "webpack xxx.js"
  }
}

// 通过 npm run start 来执行 node ./src/main.js 语句
```

> 如果是在 scripts 中添加命令，那么在使用 npm run 执行的时候会自动到当前目录下的 node-modules/.bin/ 目录下查找，找不到时才会使用全局的。这样就不需要借助 npx 来执行当前目录的命令了。（主要是为了应对版本不一致的问题）

### npm install 命令

`npm install` 安装的包会放在当前目录下的 node-modules 目录中

`npm install -g` 会进行全局安装到一个专门由 node 管理的目录，并且这个目录会被自动添加到系统的环境变量中。**一般都是安装工具包，比如 yarn**

#### 原理  

从 npm5 开始，npm 开始支持缓存策略

`npm install` 会检测是否有 `package-lock.json` 文件 

- 没有 lock 文件：
  - 分析依赖关系，因为可能会依赖其他包，并且多个包之间也会有相统依赖的情况
  - 从 registry 仓库中下载压缩包（如果设置了镜像，就是从镜像服务器中下载）
  - 获取到压缩包并对压缩包进行 **缓存** （从 npm5 开始）
  - 将压缩包解压到项目的 node-modules 文件夹中
  - 完成安装，并生成 lock 文件
- 有 lock 文件
  - 检测 lock 中包的版本是否和 package.json 中一致
    - 不一致，重新构建依赖关系，直接走顶层流程
    - 一致，则优先查找缓存
      - 找不到缓存，从 registry 仓库下载，走顶层流程，并添加缓存
      - 找到缓存，直接获取缓存中的压缩文件，并且将压缩文件解压到 node-modules 文件夹中

![npm install 原理](imgs/npm-install%E5%8E%9F%E7%90%86.png)

### npx

当当前路径下的包版本和全局的包版本不同时，使用 npx 会优先使用当前路径下的包版本

## pnpm

使用 npm 或者 yarn 时，如果有 100 个项目，并且所有项目都有同一个相同的依赖包，那么在硬盘上就需要保存 100 份该依赖包的副本，非常浪费资源

如果使用 pnpm，依赖包将被存放在一个统一的位置，因此
- 如果同一依赖包使用相同的版本，那么磁盘上只有这个依赖包的一份文件
- 如果对同一依赖包使用不同的版本，那么只有版本之间的不同文件会被存储起来
- 所有文件都在保存在硬盘上的统一位置：
  - 当安装软件包时，其包含的所有文件都会硬链接到此位置，而不会占用额外的硬盘空间
  - 这可以让我们在项目之间方便地共享相同版本的依赖包

## 版本规范

[semver](https://semver.org/lang/zh-CN)

[npm semver](https://docs.npmjs.com/misc/semver)

对于版本号 X.Y.Z 来说
- X：主版本号，当做了不兼容的 API 修改时，更新 X
- Y：次版本号，当做了向下兼容的功能性新增时，更改 Y
- Z：修订号，当做了向下兼容的问题修正（修正 bug）时，更改 Z

另外还有其他表示形式
- X.Y.Z：确定的某一个版本号
- ^X.Y.Z：X 不变，Y 和 Z 永远安装最新的
- ~X.Y.Z：X 和 Y 不变，Z 永远安装最新的 

## Webpack

> Webpack is a **static module bundler** for modern JavaScript applications. 

[官方文档](https://webpck.js.org) [中文官方文档](https://webpack.docschina.org)

**webpack 是跑在 node 环境下的**

![webpack官方图](imgs/webpack%E5%AE%98%E6%96%B9%E5%9B%BE.png)

### Node 内置模块 path

path 模块对于路径和文件处理，提供了很多好用的方法

主要用来解决不同操作系统之间对于路径之间的表示并不相同这个问题（主要是 Windows 和 类 Unix 系统）

常用 API 包括：
- 从路径中获取信息
  - `dirname` 获取文件的父文件夹
  - `basename` 获取文件名
  - `extname` 获取文件扩展名
- 路径的拼接 `path.join`
  - 如果我们希望将多个路径进行拼接，但是不同的操作系统可能使用的是不同分隔符，那么就用 join 来拼接
- 拼接绝对路径 `path.resolve` 
  - 把一个路径或路径片段的序列解析为一个绝对路径
  - 给定的路径的序列是从右往左被处理的，后面每个 path 被依次解析，知道构造完成一个绝对路径
  - 如果在处理完所有给定 path 的段之后，还没有生成绝对路径，则使用当前工作目录
  - 生成的路径被规范化并删除尾部斜杠，零长度 path 段被忽略
  - 如果没有 path 传递段，path.resolve() 将会返回当前工作目录的绝对路径

```js
const path = require("path")
const filePath = "F://Notes/test.txt"

console.log(path.extname(filePath)) // .txt
```

### Webpack 安装

- webpack
- webpack-cli（在命令行使用 webpack 要用到）
- 一般都是局部安装

![webpack和webpack-cli的关系](imgs/webpack%E5%92%8Cwebpack-cli%E7%9A%84%E5%85%B3%E7%B3%BB.png)

### 基本使用

`npm install webpack webpack-cli -D` 在当前路径下局部安装

`npx webpack` 打包，入口为 index.js 否则会报错

打包好后的文件可以在 `dist` 路径下找到，然后再 index.html 中引入 dist 目录下的打包文件 main.js，就可以正常运行了

还有一些其他参数可以设置
- `npx webpack --entry ./src/main.js` 设置入口文件，就可以不是必须有 index.js 了，也可以叫其他名字
- `npx webpack --output-path ./build` 设置打包之后的文件夹，不叫默认的 dist，叫 build
- `npx webpack --output-filename bundle.js` 那么打包好之后的文件就不叫 main.js 了，就是 bundle.js 了

**所有这些配置都可以放到一个配置文件中 `webpack.config.js`，名字最好不要改**，然后打包时直接输入 npx webpack 就可以了

如果更改了配置文件名字，那么在打包时就需要设置配置文件 `npx webpack --config 配置文件`

> 根据前面所学，可以直接将 "build": "npx webpack --config xxx.js" 放到 package.json 中的 scripts 中，并且可以省略 npx，那么就可以直接通过 npm run build
>  来进行打包了

```js
// webpack.config.js
const path = require("path")

module.exports = {
  entry: "./src/index.js",
  output: {
    // 这个路径必须是一个绝对路径，否则会报错，加 dirname 是为了防止路径出现一些问题
    path: path.resolve(__dirname, "./build"),
    filename: "bundle.js",
  },
};
```

### Webpack 依赖图

webpack 在处理程序时，会根据命令或者配置文件找到入口文件；从入口开始，会生成一个依赖关系图，这个图中会包含应用程序中所需的所有模块，比如 JS 文件，CSS 文件，图片等等；然后遍历图结构打包每个模块（本剧文件的不同使用不同的 `loader` 来解析

### loader

> 如果直接在 JS 文件中 import .css 文件会报错 "You may need an appropriate loader to handle this file type"

webpack 中的 loader 可以用于对模块的源代码进行转化

使用 loader 一共有三种方式：
- **配置方式**：在 webpack.config.js 中进行配置
- 内联方式 `import "css-loader!../css/style.css"`
- CLI 方式（webpack5 中不再使用）

#### loader 配置方式

在 module.rules 下进行配置，rules 是一个数组

> **注意：loader 的解析方式是从后向前的，也就是说先解析的 loader 要写在后面**

```js
// webpack.config.js
module.exports = {
  // ...
  module: {
    rules: [
      {
        // 告诉 webpack 匹配什么文件，必须是正则表达式
        test: /\.css$/,
        use: [
          // 只有 css-loader 只是可以加载 css 文件了，但是其中的样式并不会生效
          // 还需要 style-loader
          // 由于是从后向前解析，所以当我们需要先解析 css 再渲染 style 的时候，style-loader 就要写在前面
          { loader: "style-loader" }
          { loader: "css-loader" }，
        ]
      }
    ]
  }
}
```

简写格式如下

```js
module: {
  rules: [
    {
      test: /\.css$/,
      // 如果只有一个 loader 就可以这样写
      // loader: "css-loader"

      // 多个 loader 且没有其他参数
      use: ["style-loader", "css-loader"]
    }
  ]
}
```

### asset module type

在 webpack5 之前，加载资源我们需要使用一些 loader，比如 raw-loader、url-loader、file-loader；从 webpack5 开始，我们可以直接使用 **资源模块类型（asset module type）** 来替代 loader

- `asset/resource` 发送一个单独的文件并导出 URL（之前通过 file-loader 实现）
- `asset/inline` 导出一个资源的 data URI（之前使用 url-loader）
- `asset/source` 导出资源的源代码（之前使用 raw-loader），用的很少

asset 在导出一个 data URI 和发送一个单独的文件中间自动选择。之前通过 url-loader，并且配置资源体积限制实现

### Webpack 打包过程

[参考文章一](https://juejin.cn/post/7158707111315111972#heading-1)

1. **初始化阶段 webpack**
   1. 合并配置项
   2. 创建 `compiler`
   3. 注册插件
2. **编译阶段 build**
   1. 读取入口文件，并从入口文件开始编译
   2. 调用 `loader` 对源代码进行转换
   3. 借助 babel 解析为 AST 收集依赖模块
   4. 递归对依赖模块进行编译
3. **生成阶段 seal**
   1. 创建 chunk 对象
   2. 生成 assets 对象
4. **写入阶段 emit**

[参考文章二](https://segmentfault.com/a/1190000015088834?utm_source=sf-similar-article)

Webpack 的运行流程是一个串行的过程，从启动到结束会依次执行以下流程：

1. **初始化参数**：从配置文件和 Shell 语句中读取与合并参数，得出最终的参数；
2. **开始编译**：用上一步得到的参数初始化 Compiler 对象，加载所有配置的插件，执行对象的 run 方法开始执行编译；
3. **确定入口**：根据配置中的 entry 找出所有的入口文件；
4. **编译模块**：从入口文件出发，调用所有配置的 Loader 对模块进行翻译，再找出该模块依赖的模块，再递归本步骤直到所有入口依赖的文件都经过了本步骤的处理；
5. **完成模块编译**：在经过第4步使用 Loader 翻译完所有模块后，得到了每个模块被翻译后的最终内容以及它们之间的依赖关系；
6. **输出资源**：根据入口和模块之间的依赖关系，组装成一个个包含多个模块的 Chunk，再把每个 Chunk 转换成一个单独的文件加入到输出列表，这步是可以修改输出内容的最后机会；
7. **输出完成**：在确定好输出内容后，根据配置确定输出的路径和文件名，把文件内容写入到文件系统。

在以上过程中，Webpack 会在特定的时间点广播出特定的事件，**插件 Plugins** 在监听到感兴趣的事件后会执行特定的逻辑，并且插件可以调用 Webpack 提供的 API 改变 Webpack 的运行结果

简单说，可以分为以下三大阶段：

1. 初始化：启动构建，读取与合并配置参数，加载 Plugin，实例化 Compiler。
2. 编译：从 Entry 发出，针对每个 Module 串行调用对应的 Loader 去翻译文件内容，再找到该 Module 依赖的 Module，递归地进行编译处理。
3. 输出：对编译后的 Module 组合成 Chunk，把 Chunk 转换成文件，输出到文件系统。

如果只执行一次构建，以上阶段将会按照顺序各执行一次。但是如果在监听模式下，在文件发生变化时，文件会不停的进行 编译 -> 输出 -> 编译 -> 输出 ……

### Webpack 优化

## 代码格式化和规范检测

利用自动化手段约束规范

规范：ESLint/StyleLint/Prettier/CommitLint

自动化工具：husky/Lint-staged

| 工具 | 特点 |
| :---: | :---: |
| ESLint/StyleLint | 1. 静态代码分析工具<br>2. 帮助开发者 **提前发现并修复** 代码中的问题<br>3. 让开发者能够写出更健壮、可维护和符合规范的代码 |
| Prettier | 1. 代码格式化工具<br>2. 目标是 **消除代码样式上的争议**，使得整个项目的代码 **风格保持一致** |
| CommitLint | 1. **规范化 Git 提交信息**，确保每次提交都符合预定义的规范<br>2. 开发团队可以统一提交信息的格式，使得 **提交历史更加清晰、易于阅读**，并有利于自动化工具进行后续处理，如自动生成版本号、生成变更日志等 | 

| 工具 | 特点 |
| :---: | :---: |
| husky | 1. Git 钩子工具，在 Git 事件发生时执行预定义的脚本<br>2. **自动化** 的代码质量控制和规范化操作<br>3. 适用于代码格式化、静态代码分析、单元测试 |
| Lint-staged | 1. 允许在 **Git 暂存区（Staging Area）** 中的文件上运行特定的任务<br>2. 提高效率，只对 **修改的文件** 进行必要的处理，而不是每次都对整个代码库执行脚本 |

![prettier配置示例](imgs/prettier%E9%85%8D%E7%BD%AE%E7%A4%BA%E4%BE%8B.png)

## 如何开发一个组件

如何开发一个通用组件呢？

实现一个完整的组建项目需要包含什么能力？

- 有开发环境，你得起个服务去调试代码吧，同时能够有组件预览，让使用者看到组件具体长什么样子吧
- 源码仓库与测试仓库分离，但是可以统一构建、跨 Package 调试 (**Monorepo**)
- 组件库编译，生成 und、cmd 和 esm 模块的组件代码（**模块化规范与构建工具**）
- 完善的代码格式化和规范检测等自动化功能(**ESLINT、 CommitLint. Prittier. husky**)
- 完善的使用文档，至少得有个中文文档说明一下组件怎么用吧 （**完善的 readme.md CHANGELOG.md**)
- 单元测试，你写的代码得信的过吧
- NPM 包产物、自动化的文档部署和测试流程，总不能每次发布版本都手动去部署文档和测试吧 (**NPM发布、同步提交、ChangeLog、自动打包**）